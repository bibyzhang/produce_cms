$(function() {
	if (window.PIE) {
		$('.gameStatistics strong').each(function() {
			PIE.attach(this);
			});
		$('.list-9 li .rankingDownloadIcon').each(function() {
			PIE.attach(this);
			});	
		$('.gameBrief .gbi-button a').each(function() {
			PIE.attach(this);
			});
		$('.singelPage .downloadIcon').each(function() {
			PIE.attach(this);
			});	
		$('.list-2 li img').each(function() {
			PIE.attach(this);
			});
		$('.list-4 img').each(function() {
			PIE.attach(this);
			});
		$('.iOSGamePage .iosrPoint a span img').each(function() {
			PIE.attach(this);
			});
		$('.list-13 li a img').each(function() {
			PIE.attach(this);
			});	
		$('.list-9 li .rankingImg').each(function() {
			PIE.attach(this);
			});	
		$('.list-11 li a img').each(function() {
			PIE.attach(this);
			});	
		$('.gameBrief-Icon img').each(function() {
			PIE.attach(this);
			});
	}
});